# Series Style Manifest Template v0.8.10

Complete this block once and copy it unchanged into every page prompt.

```text
SERIES ID: {series_id}

VERBATIM SERIES STYLE BLOCK — COPY UNCHANGED:
Use exactly one visual system across this entire series.

CHINESE TYPOGRAPHY: {typography_family_token}. Use the same upright character skeleton, rounded stroke terminals, stroke thickness, numeral construction, punctuation and spacing rhythm on every page. Cover title uses the same family extra-bold; content-page title uses the same family bold; body, chart labels and small labels use the same family regular. Maximum one family and three levels. Never use brush calligraphy, printed serif/song type, bubble-outline lettering, thin elegant script or a second handwriting family.

ILLUSTRATION AND ICON GRAMMAR: {icon_grammar_token}. Keep one stable medium soft-black outline, rounded joins, one flat 2D perspective system, one simple editorial detail band, low-saturation pastel fills and sparse paper texture. No gradients, glossy rendering, 3D, emoji, clip art or photorealistic shadows.

CHART GRAMMAR WHEN USED: {chart_grammar_token}. Axis token {axis_token}; primary series {primary_series_token}; secondary series {secondary_series_token}; labels use the same body typography token. No dashboard chrome, dense spreadsheet grid or alternate chart style.

PALETTE: background {background_token}; ink {ink_token}; accents only {accent_1}, {accent_2}, {accent_3}.

COMPONENTS: callouts {callout_shape}; highlights {highlight_shape}; dividers {divider_style}; corner radius {corner_radius_token}. Reuse these components rather than inventing new boxes, banners or stickers.

LAYOUT: outer safe margin {safe_margin}; title zone {title_zone}; content zone {content_zone}; footer zone {footer_zone}; alignment {alignment_token}.

REPEATED ICONS — USE EXACT DESCRIPTIONS:
{repeated_icon_dictionary}

REPEATED CHARTS — USE EXACT DESCRIPTIONS:
{repeated_chart_dictionary}

Every page must look lettered and drawn by the same illustrator using the same pen, handwriting family, numeral shapes, line weight, icon library, chart library, palette and component system as the approved anchor page.
```
