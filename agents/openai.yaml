interface:
  display_name: "涂鸦卡片"
  short_description: "Codex 原生 3:4 中文涂鸦卡片"
  default_prompt: "Use $xingchen-doodle-card-skill v0.8.10. Treat 3:4 and any example dimensions only as an aspect-ratio request, never as a fixed-pixel request. Generate strictly P01 then P02 onward with Codex $imagegen, validate each returned file by its actual width/height ratio, accept any native size whose ratio is 0.75 within tolerance, discard and regenerate non-3:4 results without padding/crop/resize, checkpoint every completed page, then independently revalidate and package continuous ordered filenames."
